# Implementation Plans

Generated by the improve skill on 2026-09-01. Planned at commit `70e35c2`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Enforce allow-list on hardware audit rollback field | P1 | S | — | TODO |
| 002 | Deny hardware restore when organizational scope cannot be determined | P1 | S | — | TODO |
|| 003 | Restore bulk audit suppression flag with try/finally | P2 | S | — | DONE |
| 004 | Use request() helper instead of Request::capture() in HardwareAuditObserver | P2 | S | — | TODO |
| 005 | Normalize Persian and escape LIKE wildcards in hardware export | P2 | S | — | TODO |
| 006 | Reset Postgres sequence after restoring hardware with explicit id | P3 | S | 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 006 requires 002 because both touch `app/Http/Controllers/Api/HardwareAuditController.php` (`restoreRecord` and `assertAccessibleFromAudit` are adjacent). Apply 002 first or rebase; if done in parallel, the second to merge must handle the merge conflict in that file. No other ordering required — 001, 003, 004, 005 are independent.
- 001 and 002 both add scope checks but on different methods (`rollback` vs `restoreRecord`) — no conflict.
- 003 touches `HardwareController` only — no conflict with audit controller plans.

## Findings considered and rejected

- `LIKE "%{$s}%"` as SQL injection — rejected: bound as value by query builder; real issue is wildcard semantics (captured as 005).
- `HardwareExport` column injection — rejected: `app/Exports/HardwareExport.php:51` allow-lists via `array_intersect($columns, array_keys($columnDefs))`.
- `Boundary::guarded=[]` — not a quick hotspot; defer to standard audit.
- `npm audit` / `composer audit` — 0 advisories (verified).

## Policy compliance (AGENTS.md)

All plans below were re-checked against **AGENTS.md** general policies — no code was executed (`plans/` only, per user request):

- **New Features workflow**: GitHub issue → branch from `kimya`/`main` → develop → test → PR; every plan lists its branch and keeps `it-test5` untouched.
- **RTL / Persian**: project is fully RTL (`dir="rtl"`, Vazirmatn, Jalalian). Plans keep UI untouched (API-only) and preserve Persian labels (`بازگردانی`, source badges `وب/API/ایمپورت/گروهی`) — no layout/CSS change.
- **MaryUI / Livewire**: no UI change; if UI were touched, must use single-file `resources/views/livewire/<feature>/<name>.blade.php` + DaisyUI v5/maryUI v2.8 components (AGENTS.md **Design System**). Plans stay out of scope.
- **Access control**: every hardware write keeps `spatie/laravel-permission` `manage_hardware` + `AccessService::accessibleUnitIds()` + `HasOrganizationalScope` (AGENTS.md **Access Control** / **Guidelines #5-6**).
- **Persian normalization & Jalali**: `PersianNormalizer::normalizeForSearch` on all filters (plan 005) and `Jalalian::fromCarbon` for audit dates (existing).
- **Cache**: `CacheInvalidationService` version counters (`hardware_stats/gis/maps/dashboard`) bumped via `Hardware::flushStatsCache()` — plans keep that, no raw `Cache::flush`.
- **Testing & formatting**: verification is `composer test` (which does `config:clear + route:clear + XDEBUG_MODE=off php artisan test`, hermetic `CACHE_STORE=array`, 884 baseline, `h_dashboard_test` on pgsql) + `vendor/bin/pint --dirty --format agent` (AGENTS.md **Running Tests / Conventions**).
- **Migrations**: no new migration in these 6 S-sized fixes; if added, use `YYYY_MM_DD_######` + `--no-interaction` (AGENTS.md **Conventions > Artisan**).
- **Code intelligence**: executor must run `codegraph explore "<symbols>"` before editing (AGENTS.md **Code Intelligence — Hermes MUST use CodeGraph**).
- **Frontend/deps**: `npm install` + `npm run build` only after frontend changes; only `package-lock.json` committed, no `pnpm-lock.yaml`. Plans are backend-only.
- **Scheduler note**: `zabbix:sync` must not use `->timeout(N)` (AGENTS.md gotcha) — untouched.

## How to execute

- One plan per branch: `advisor/00N-<slug>` per plan's Git workflow section.
- Verification baseline for all plans: `composer test` (≈884 passed at 70e35c2) + `vendor/bin/pint --dirty --format agent`.
- Update this table's Status column when you finish a plan. Do not execute now — this update was plans-only.
