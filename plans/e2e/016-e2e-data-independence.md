# Plan 016 — E2E Data Independence (خودکفایی تست‌های E2E)

**Priority:** P0 · **Status:** planned · **Depends on:** none (infrastructure first)

## Why this matters (دلیل)

تست‌های E2E فعلی رفتار اپ را تست نمی‌کنند؛ اسنپ‌شات دیتای امروزند. سه وابستگی
شکننده به دیتای واقعی دارند:

1. **عدد مطلق** — `317` یوزر، `318` پرسنل، `831` واحد، `449` سخت‌افزار،
   `290/241/49` در گزارش‌ها. هر insert/delete ساده می‌شکندشان.
2. **رکورد واقعی** — جستجوی `هادیلو`، `عسگری`، کدهای ملی واقعی. حذف یا تغییر
   آن شخص = شکست تست.
3. **اکانت مشترک** — همه تست‌ها با ۴ اکانت سیدشده لاگین می‌کنند؛
   `password-change.spec.ts` رمز اکانت مشترک را عوض می‌کند و برمی‌گرداند
   (اگر وسطش بمیرد، رمز خراب می‌ماند).

نتیجه: با هر تغییر دیتا، تست‌ها می‌شکنند و به‌مرور نادیده گرفته می‌شوند.
هدف این پلن: هر ران دیتای خودش را بسازد، آخرش پاک کند، و assertها نسبی باشند.

## Principles (اصول)

- هر ران دیتای خودش را با **پیشوند یکتا** (`E2E-<runId>-...`) بسازد، آخرش پاک کند.
- assertها **نسبی** باشند (قبل/بعد، وجود رکورد ساخته‌شده) نه عدد ثابت.
- هیچ کر دن شل واقعی در فایل ترک‌شده نباشد؛ فقط از `.env.test` (ایگنورشده).
- تست‌های Pest دست نخورند؛ رمز سیدرها (`12345678`) هم عوض نشود.

## Architecture (معماری)

```
playwright.config.ts
  ├── globalSetup → ساخت یوزرها + دیتای حداقلی (runId یکتا)
  ├── تست‌ها → فقط با دیتای runId خودشان کار می‌کنند
  └── globalTeardown → حذف همه رکوردهای E2E-<runId>
```

- یک کامند آرتیزان جدید `e2e:fixtures {runId} {action=up|down}` که منطق
  ساخت/حذف در PHP بماند (مدل‌ها، ولیدیشن، نقش‌ها = همان منطق واقعی).
  صدا زدن از `globalSetup` با `execSync`.
- رمز یوزرهای موقت از `TEST_PASSWORD` در `.env.test` بیاید، نه هاردکد.
- `fixtures.ts` فقط لاگین با همین یوزرهای موقت.

## Phase 1 — زیرساخت (بدون تغییر تست‌ها)

1. `app/Console/Commands/E2eFixturesCommand.php`: با `runId` چهار یوزر
   (admin/unit_manager/expert/user) + یک واحد تستی + چند پرسنل تستی با
   نام‌های `E2E-<runId>-...` می‌سازد؛ با `down` همه را پاک می‌کند.
2. `tests/e2e/global-setup.ts` و `global-teardown.ts`: تولید `runId`، صدا زدن
   کامند، ذخیره `runId` در فایل موقت برای teardown.
3. `playwright.config.ts`: وصل کردن setup/teardown + fail-fast اگر
   `TEST_PASSWORD` ست نباشد.
4. **Verify:** کل سوئیت باید مثل قبل سبز شود
   (`npx playwright test` — هنوز assertها قدیمی‌اند ولی دیتا سر جاش است).

## Phase 2 — نسبی کردن assertها (فایل به فایل)

| فایل | مشکل | راه‌حل |
|---|---|---|
| `users/list.spec.ts` | `317`، `32`، `هادیلو`، `0023548258` | شمارش قبل/بعد؛ جستجو روی پرسنل `E2E-<runId>` ساخته‌شده |
| `personnel/list.spec.ts` | `318`، `4411015056`، ایندکس select (`بیسواد`/`دیپلم`) | `318` → `>0` + رفتار فیلتر (کم شدن/برگشتن)؛ جستجو روی رکورد ساخته‌شده؛ select با value مشخص نه index |
| `organization/units.spec.ts` | `831`، `دانشگاه علوم پزشکی زنجان` | جستجو روی واحد `E2E-<runId>`؛ paginate رفتاری |
| `hardware/list-filters.spec.ts` | `449` | فیلتر روی سخت‌افزار ساخته‌شده با سریال یکتا |
| `reports/map-no-boundary.spec.ts` | `290/241/49` | ساخت واحد بدون مرز → عدد یکی زیاد شود، teardown برگردد |
| `dashboard/stats.spec.ts` | کامنت اعداد ثابت | فقط presence و سازگاری (جمع اجزا = کل) |
| `tickets/new.spec.ts` | تیکت `تست خودکار E2E` بدون پاکسازی | subject یکتا با runId + حذف در teardown |
| `auth/password-change.spec.ts` | جهش روی اکانت مشترک | اجرا روی یوزر موقت اختصاصی همین اسپک |
| `rbac/roles.spec.ts` | وابسته به ۴ اکانت سیدشده | لاگین با ۴ یوزر ساخته‌شده در setup |
| `users/crud.spec.ts` | `هادیلو` | ساخت/ویرایش/حذف یوزر `E2E-<runId>` و assert روی همان |

## Phase 3 — اثبات پایداری (verification gates)

1. `grep -rEn "toContainText\(['\"][0-9]{2,}" tests/e2e/` → صفر نتیجه (عدد ثابت نمانده).
2. `npx playwright test` → سبز.
3. تست ضربه: یک یوزر/پرسنل دستی اضافه و کم کن، دوباره اجرا → همچنان سبز.
4. `git status` → هیچ فایل حاوی کر دن شل واقعی ترک نشده
   (فقط `.env.test` ایگنورشده و `.env.test.example` بدون مقدار).

## Risks & decisions (ریسک‌ها)

- **دیتابیس جدا vs مشترک:** ساده‌ترین حالت همین DB فعلی + پیشوند یکتا و
  teardown است. دیتابیس E2E جدا تمیزتر است ولی نیاز به `serve` با env جدا و
  سید پایه دارد — فاز بعدی (Phase 4) اگر لازم شد.
- **پارالل:** چون `fullyParallel` است، یوزرها باید per-run باشند نه per-test
  تا لاگین‌ها تداخل نکنند؛ فقط `password-change` روی اکانت اختصاصی خودش.
- **حجم کار:** ~۳۲ فایل، ولی تغییرات مکانیکی و تکراری‌اند؛ فاز ۱ که آماده
  شود هر فایل چند خط است.

## Already done (پیش‌نیاز انجام‌شده — همین PR)

- `tests/e2e/shared/fixtures.ts`: fallback هاردکد `12345678` حذف شد؛
  `TEST_PASSWORD` از `.env.test` خوانده می‌شود و اگر ست نباشد خطای واضح می‌دهد.
- `tests/e2e/users/crud.spec.ts`: `fill('12345678')` → `TEST_USER.password`.
- `.env.test.example` (بدون مقدار واقعی، قابل کامیت) + `.env.test` لوکال
  (ایگنورشده) ساخته شد.
